
function addCart(product,productImg,user,userImg){
    console.log(user.typeof)
    var productData=JSON.parse(product);
    if(user){
        var userData=JSON.parse(user);
    }

    console.log(productData)
}
